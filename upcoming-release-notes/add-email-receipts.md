---
category: Features
authors: [AI]
---

Add Email Receipts to the hub: connect Gmail read-only via a Google OAuth loopback flow, extract receipt emails with a local LLM (Ollama, nothing leaves the machine), and match them to imported transactions - exact-amount slam dunks auto-apply as itemized splits or payee/notes enrichment (logged and undoable), everything else lands in a review queue
